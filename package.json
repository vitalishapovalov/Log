{
  "name": "js-utilities",
  "private": true,
  "version": "0.1.0",
  "description": "JavaScript utilities library",
  "license": "MIT",
  "author": {
    "name": "Vitalii Shapovalov",
    "email": "<shapovalov.vitali.v@gmail.com>"
  },
  "scripts": {
    "clean": "rm -rf ./node_modules && lerna clean --yes && lerna exec -- rm -rf .rpt2_cache && lerna exec -- rm -rf coverage",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "cleanBuild": "npm run clean && npm run bootstrap && npm run build",
    "prepublishOnly": "npm run cleanBuild && npm run lint && npm run test"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.0.12",
    "lerna": "3.15.0",
    "rollup-plugin-commonjs": "^10.0.0",
    "rollup-plugin-node-resolve": "^5.0.3",
    "rollup-plugin-sourcemaps": "^0.4.2",
    "rollup-plugin-terser": "^5.0.0",
    "rollup-plugin-typescript2": "^0.21.2",
    "ts-jest": "^24.0.2",
    "tslint-config-airbnb": "^5.11.1",
    "typescript": "^3.5.2"
  },
  "greenkeeper": {
    "ignore": ["lerna"]
  }
}
